<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">

    <!-- Load environment specific YAML -->
    <configuration-properties file="config/${mule.env}.yaml"/>

    <!-- HTTP Listener -->
    <http:listener-config name="HTTP_Listener_config">
        <http:listener-connection host="0.0.0.0" port="8081"/>
    </http:listener-config>

    <flow name="env-login-flow">

        <http:listener path="/test" config-ref="HTTP_Listener_config"/>
        <ee:transform>
            <ee:message>
                <ee:set-payload>
                    <![CDATA[
                    %dw 2.0
                    output application/json
                    ---
                    {
                      username: "${username}",
                      password: "${password}",
                      env: "${mule.env}"
                    }
                    ]]>
                </ee:set-payload>
            </ee:message>
        </ee:transform>

        <!-- Simple Authentication Logic -->
        <choice>
            <when expression="#[p('username') == 'eve.holt@reqres.in' and p('password') == 'cityslicka']">

                <ee:transform>
                    <ee:message>
                        <ee:set-payload>
                            <![CDATA[
                            %dw 2.0
                            output application/json
                            ---
                            {
                              status: "SUCCESS",
                              token: "abc123",
                              message: p('app.message')
                            }
                            ]]>
                        </ee:set-payload>
                    </ee:message>
                </ee:transform>

            </when>

            <otherwise>
                <set-payload value='{"status":"FAILED","message":"Invalid credentials"}'
                             mimeType="application/json"/>
            </otherwise>
        </choice>

        <logger level="INFO" message="#[payload]"/>

    </flow>

</mule>