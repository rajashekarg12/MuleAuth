<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns:secure-properties="http://www.mulesoft.org/schema/mule/secure-properties"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd
        http://www.mulesoft.org/schema/mule/secure-properties http://www.mulesoft.org/schema/mule/secure-properties/current/mule-secure-properties.xsd">

    <!-- Load YAML -->
    <configuration-properties file="config/${mule.env}.yaml"/>

    <!-- Secure Properties (same YAML file) -->
    <secure-properties:config
            name="Secure_Config"
            file="config/${mule.env}.yaml"
            key="${secure.key}">
        <secure-properties:encrypt algorithm="Blowfish" mode="CBC"/>
    </secure-properties:config>

    <!-- HTTP Listener -->
    <http:listener-config name="HTTP_Listener_config">
        <http:listener-connection host="0.0.0.0" port="8081"/>
    </http:listener-config>

    <flow name="auth-flow">

        <http:listener path="/login"
                       config-ref="HTTP_Listener_config"
                       allowedMethods="GET"/>

        <!-- Read query parameters from browser -->
        <set-payload value="#[
            {
                username: attributes.queryParams.username,
                password: attributes.queryParams.password
            }
        ]"/>

          <logger level="INFO"
        message="#['Incoming username: [' ++ payload.username ++ ']']"/>

<logger level="INFO"
        message="#['Incoming password: [' ++ payload.password ++ ']']"/>
          
        <!-- Logging -->
        <logger level="INFO"
                message="#['Runtime username: ' ++ p('username')]"/>

        <logger level="INFO"
                message="#['Decrypted password: ' ++ p('secure::user.password')]"/>

        <!-- Authentication Logic -->
        <choice>

              <when expression="#[
    upper(trim(payload.username)) == upper(trim(p('username')))
    and
    trim(payload.password) == trim(p('secure::user.password'))
]">

            <otherwise>

                <set-payload value='{"status":"FAILED","message":"Invalid credentials"}'
                             mimeType="application/json"/>

            </otherwise>

        </choice>

    </flow>

</mule>
